# NNRP/1 版本定位

当前用户面文档只保留一个主入口：`/zh/protocol/v1/`。

这里的“预览”只表示正式版尚未发布，不表示还要让使用者在多个 preview 编号之间挑入口。

当前规则：

1. 第一次阅读、当前接入和对外沟通都从 `NNRP/1（预览）` 开始。
2. 版本编号页不再作为用户主导航的一部分。
3. 更早的设计演进放在“协议设计”索引，而不是放在协议主路径里。
