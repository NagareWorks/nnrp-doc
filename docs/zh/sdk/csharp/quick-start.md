# C# 快速使用

这一页保留给 C# SDK 的对外快速使用入口。

## 当前状态

1. 文档结构已经预留，但公开示例和 NuGet 接入流程暂不展开。
2. 在 SDK 正式对外开放前，这里只维护稳定入口和文档位置，不写临时宿主 wiring。

## 后续应放置的内容

1. 包安装方式与目标框架要求。
2. 最小 client 构造示例。
3. 会话打开、提交、接收、关闭的最短路径。
4. 常见异常与最小排障提示。