# Python 冻结 API

Python SDK 在 Preview3 集成窗口内应冻结一组明确的控制面接口。

## 核心能力面

1. 负责端点、传输策略和凭据的客户端构造入口。
2. 会话打开、补丁、关闭和迁移方法。
3. 操作提交与接收循环入口。
4. 缓存与 Schema 生命周期方法。
5. 稳定的错误层级与取消接口。

## Python 侧约束

1. 异步方法应作为主契约。
2. 同步封装可以存在，但只是同一控制面语义的便捷层。
3. 公开方法名、参数分组和返回状态对象不应在未升级 SDK 版本的情况下漂移。